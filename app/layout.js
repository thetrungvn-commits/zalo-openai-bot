export const metadata = {
  title: "Zalo OpenAI Bot",
  description: "Chatbot Zalo OA sử dụng OpenAI",
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  );
}
